<x-container>
    <div class="space-y-4 py-10">
        <a href="{{route('home')}}" class="font-bold text-3xl">{{config('app.name')}}</a>
        <p>2023 - все права защищены</p>
    </div>
</x-container>
